[build-system]
requires      = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name        = "pycommon"
version     = "0.1.0"
description = "Shared utilities for the backend stack"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "src"}
